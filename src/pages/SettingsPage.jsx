import React from 'react';
import {useTitle} from "../hooks/useTitle";

export const SettingsPage = () => {
  useTitle('Student Fee Management Tab');
  return (
    <div>
      <h1>No Setting Assigned Yet </h1>
    </div>
  );
};